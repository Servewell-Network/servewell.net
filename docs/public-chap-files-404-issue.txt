The problem may have originally been caused by a stray async neighborScripts without await.
This caused it to deploy while public/-/ was deleted, and this made cloudflare cache some do-not-serve flag
But re-deploy was not always enough to fix it. (But sometimes it was enough.)

I'm avoiding the frequently-offered "fix" of replacing the 404 line in index.ts 
    with a delegation to assets.
    I don't want to mask the problem and risk running my worker on every request 
    from around the globe. I want to try other things first. 
    
Here are the main possibilities in my mind, starting with the one I plan to try first (Apr 18):

running wrangler deploy when there are no other changes to deploy
    may cause cloudflare to realize that the public/-/ files really are intended to be deployed
wrangler/cloudflare may be respecting my .gitignore somehow
moving .assetsignore to the root may cause it to have more priority
making .assetsignore truly empty may cause it to have more priority
all else fails, adding a separate deploy line to pre-deploy that points 
    specifically to public/-/ could cause wrangler/cloudflare to ignore the .gitignore
cloudflare could be caching the rule to not serve the chapter pages it has, 
    causing the logs to say 'Read 1284' and '1210 already uploaded' but then not serve those files
the cache can perhaps be cleared in the cloudflare dashboard or 
    by arbitrarily introducing a throw-away file into public/-/
    Claude: in the Cloudflare dashboard you can purge cache for servewell.net/-/* 
    by going to Caching → Cache Rules → Purge Cache.